package kanagawa.models.enums;

/**
 * Bonuses are the extra items that the personal work part of a card can
 * contains.
 */
public enum Bonus {
    PEN,
    PROFESSOR,
    CREDIT,
    DOUBLE_CREDIT,
    NONE;
}
